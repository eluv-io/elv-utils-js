// builds (or rebuilds) the main.js file
'use strict'

const fs = require('fs')
const path = require('path')

const {buildDir} = require('../utilities/lib/helpers')

const fileContents = `// This file is auto-generated by ./bin/buildMain.js

const {runUtility} = require('./lib/helpers')

module.exports = {
  runUtility
}`

fs.writeFileSync(path.join(buildDir, 'main.js'), fileContents)
